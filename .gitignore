node_modules
dist
out
.DS_Store
.eslintcache
*.log*

# Electron builder
release/

# Cache de dados local (gerado automaticamente pelo app)
public/data.json

# Variáveis de ambiente
.env
.env.*
!.env.example

# Nunca deveria existir aqui (credencial da Service Account é userData/, fora
# do repo), mas se alguém colar por engano dentro do projeto, não vai commitar
credentials.json
credentials.enc
*.pem
service-account*.json

# Editor
.vscode/settings.json
*.suo
*.user
